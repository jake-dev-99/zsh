# -----------------------------------------------------------------------------
# .bash_profile
# -----------------------------------------------------------------------------
# Login shell entry point for Bash.
# Loads the interactive Bash configuration from ~/.bashrc.
# -----------------------------------------------------------------------------

# Source the interactive Bash configuration if it exists.
[[ -r "$HOME/.bashrc" ]] && source "$HOME/.bashrc"
